import React, {Component} from 'react';
import {Platform, StyleSheet, Text, View} from 'react-native';
import EmailAndPass from '../../Components/2.EmailAndPass/index'

export default class EmailAndPassContainer extends Component  {
  render() {
    return (
      <EmailAndPass />
    );
  }
}